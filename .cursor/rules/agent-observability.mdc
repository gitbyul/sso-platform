---
description: 메트릭, Actuator, MDC, JSON 로그 규칙
globs: **/*.{java,properties,yml,xml}
alwaysApply: false
---

# 메트릭

- `sso.auth.attempts`, `sso.auth.duration`, `sso.token.issued`, `sso.token.revoked`
- `sso.session.active`, `sso.mfa.challenges`, `sso.login.failures`
- `sso.key.rotations`, `sso.rate_limit.exceeded`, `sso.audit.persistence.failures`
- 태그는 최소 `tenant`와 도메인별 핵심 차원을 포함한다.

# Actuator

- `management.server.port=8081`로 분리한다.
- 노출 엔드포인트는 `health,info,prometheus,metrics,loggers` 중심으로 제한한다.
- 내부망 접근 제어 정책과 함께 운영한다.

# MDC

- 표준 키: `traceId`, `spanId`, `tenantId`, `userId`, `clientId`
- 임의 MDC 키 추가를 피하고 표준 키만 사용한다.

# 로그 포맷

- 구현: `sso-bootstrap/src/main/resources/logback-spring.xml` — `docker`, `production`, `dev`, `staging` 은 Spring Boot `structured-console-appender` + `logging.structured.format.console=logstash`(한 줄 JSON). `local`(및 위 네 프로파일이 모두 비활성인 경우)은 텍스트 콘솔 + `application-local.properties` 의 `logging.pattern.console`(MDC 표준 키 반영).
- **JSON:** `docker`, `production`, `dev`, `staging`.
- **텍스트:** `local` (워크스테이션에서 Compose 인프라만 띄우고 JVM으로 앱 실행하는 기본 패턴).
