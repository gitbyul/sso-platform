# 스테이징: SPRING_PROFILES_ACTIVE=staging — 운영과 동일한 설정/보안 절차, 값만 스테이징 전용 인스턴스
# DB·Redis·Vault·토큰은 배포 파이프라인/시크릿 매니저로 주입 (본 파일에 자격증명 금지)

spring.jpa.hibernate.ddl-auto=none

spring.flyway.out-of-order=false

logging.structured.format.console=logstash
